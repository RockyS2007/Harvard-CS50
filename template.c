#include <stdio.h>
#include "cs50.h"

/*
for windows
gcc -o file file.c cs50.c
.\file

for Apple silicon
clang file.c -lcs50 -o file
(gcc also works if its installed with homebrew)
*/

int main (void) 
{
    
}